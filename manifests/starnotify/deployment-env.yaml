apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: not-important
spec:
  template:
    spec:

      containers:
      - name: starnotify

        envFrom:
        - secretRef:
            name: credentials
