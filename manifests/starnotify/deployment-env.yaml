apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
spec:
  template:
    spec:

      containers:
      - name: starnotify

        envFrom:
        - secretRef:
            name: credentials
